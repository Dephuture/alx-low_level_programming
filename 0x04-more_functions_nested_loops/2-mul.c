#include "main.h"
/**
 * mul - checks whether is upper
 * @a: checks the character
 * @b: checks the character
 * Return: returns 0 when upper  and 1 when not
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
