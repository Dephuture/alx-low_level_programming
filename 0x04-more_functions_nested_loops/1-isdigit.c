#include "main.h"
/**
 * _isdigit - checks whether is upper
 * @c: checks the character
 * Return: returns 0 when upper  and 1 when not
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
