#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - To print true and false
 * description: kkookoi
 * Return: always 0
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
		
	return (0);
}
