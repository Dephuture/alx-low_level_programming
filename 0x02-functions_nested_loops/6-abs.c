#include "main.h"
/**
 * _abs - this task is part of the function projects
 * @i: letter
 * Return: Always return 0 for success
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
