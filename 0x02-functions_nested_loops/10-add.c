#include "main.h"
/**
 * add - this task is part of the function projects
 * @a: the first integer
 * @b: the second interger
 * Return: Always return 0 for success
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
