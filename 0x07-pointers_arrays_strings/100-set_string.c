#include "main.h"
#include <stddef.h>
#include <stdio.h>
/**
 * set_string - works as memset
 * @s: takes an int
 * @to: takes int
 * Return: returns 0 when successful
 */

void set_string(char **s, char *to)
{
	*s = to;
}
